import { Typography } from "@mui/material";

export default async function ForgotPasswordPage() {
    return <Typography variant="h4" color="tertiary" sx={{ display: "flex", justifyContent: "center", alignItems: "center", height: "100vh" }}>Forgot Password Page</Typography>;
}
